apiVersion: apps.gitlab.com/v1beta1
kind: GitLab
metadata:
  name: gitlab
spec:
  chart:
    version: "9.6.2" # https://gitlab.com/gitlab-org/cloud-native/gitlab-operator/-/blob/<OPERATOR_VERSION>/CHART_VERSIONS
    values:
      global:
        hosts:
          domain: ingress-gpu02.lysdemolab.fr # use a real domain here
        ingress:
          configureCertmanager: true
      certmanager-issuer:
        email: jerome.armand@hpe.com # use your real email address here
